#if !defined(SERIES_H)
#define SERIES_H
#include <stdexcept>
#include <string>
#include <vector>

namespace series {
std::vector<std::string> slice(std::string number, int length);
} // namespace series

#endif // SERIES_H
